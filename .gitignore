node_modules/
dist/
.env
.env.*
!.env.example
!.env.production.example
*.log
coverage/
.DS_Store
prisma/*.db
# runtime user-file storage at the repo ROOT only — unanchored `uploads/` also swallowed the
# src/modules/uploads source module (CI-breaking: fresh clones missed it entirely)
/uploads/

# SECRETS — never commit
firebase-service-account.json
*.pem
*.p8
